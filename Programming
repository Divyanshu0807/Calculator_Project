#include <stdio.h>

int main()
{
int i; int factorial=1; int number;
printf("enter the number\n");
scanf("%d",&number);
for(i=1;i<=number;i++){
    factorial=factorial*i;
}
printf("the factorial of %d is %d\n",number,factorial);
    return 0;
}
